#settings.py

#Settings file to include server, user, password information to be used 
#in veeva vault document upload interface.

#This is needed to avoid hardcoding values into the main script
# username = <enter username>
# password = <enter password>
# domain_name = <enter you vault domain name>
# version = <set your version>
# directory = <set directory where files will be coming from>